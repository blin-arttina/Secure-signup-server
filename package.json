
{
  "name": "secure-signup-server",
  "version": "1.0.0",
  "description": "Secure beta tester sign-up storage server",
  "main": "server.js",
  "scripts": {
    "start": "npm install && node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "fs": "0.0.1-security",
    "cors": "^2.8.5"
  }
}
